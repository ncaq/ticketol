<h1>Top#show</h1>

<article>
  <h1>新着コンサート</h1>
  <ul>
    <% @concerts.each do |concert| %>
      <li>
        <figure>
          <%= image_tag image_concert_path(concert) %>
          <figcaption>
            <%= concert.title %>
            <%= concert.artist %>
            <%= concert.user.email %>
            <%= link_to 'Show', concert %>
          </figcaption>
        </figure>
      </li>
    <% end %>
  </ul>
</article>
